import {
    generateSecret,
    generatedSecret,
    signToken,
    verifyTokenMiddleware,
} from "./jwt";
import { ErrorHandler } from "./ErrorHandler";

export {
    generateSecret,
    generatedSecret,
    signToken,
    verifyTokenMiddleware,
    ErrorHandler,
};
export default {
    generateSecret,
    generatedSecret,
    signToken,
    verifyTokenMiddleware,
    ErrorHandler,
};
