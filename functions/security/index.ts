import {
    hashData,
    hashDataWithRandomSalt,
    hashPassword,
    verifyHash,
    verifyPassword,
} from "./Hash";
import { encryptData, decryptData } from "./Encrypt";

export {
    // Hashing
    hashData,
    verifyHash,
    hashDataWithRandomSalt,
    hashPassword,
    verifyPassword,
    // Encryption
    encryptData,
    decryptData,
};

export default {
    // Hashing
    hashData,
    verifyHash,
    hashDataWithRandomSalt,
    hashPassword,
    verifyPassword,
    // Encryption
    encryptData,
    decryptData,
};